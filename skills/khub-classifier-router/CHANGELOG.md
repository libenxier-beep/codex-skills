# Changelog

## v0.1

- Created the initial KHub classifier/router skill skeleton.
- Locked output states into `route-plan` and `decision-report`.
- Added explicit write gating with `no_fit`, `needs_human_decision`, and `write_blocked`.
